{
  "name": "executable-stories-live-demo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Live demo: Playwright story tests → a hosted, media-bundled executable-stories report.",
  "scripts": {
    "test": "playwright test",
    "generate": "executable-stories format reports/raw-run.json --format html --asset-mode copy --output-dir dist --output-name index",
    "build": "playwright test || true; pnpm run generate",
    "preview": "npx --yes serve dist"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "executable-stories-formatters": "^0.17.0",
    "executable-stories-playwright": "^8.5.6",
    "typescript": "^5.9.3"
  }
}
